---
import { Image } from "astro:assets";
import LogoImage from "@/assets/colored-logo.svg";
import PeopleImage from "@/assets/kv-people.png";
import TrafficImage from "@/assets/kv-traffic.png";
import TileImage from "@/assets/kv-shikxui-bg.png";

import TrafficSpImage from "@/assets/kv-traffic-sp.png";
import PeopleSpImage from "@/assets/kv-people-sp.png";
---

<section id="kv" class="container">
  <div class="contents">
    <div class="bgColor bgBase"></div>
    <picture>
      <source srcset={TrafficImage.src} media="(min-width: 767px)" />
      <img
        src={TrafficSpImage.src}
        width="767"
        height="767"
        alt=""
        class="traffic bgBase"
      />
    </picture>
    <Image src={TileImage} alt="" class="tile bgBase" layout="full-width" />
    <picture>
      <source srcset={PeopleImage.src} media="(min-width: 767px)" />
      <img
        src={PeopleSpImage.src}
        width="767"
        height="767"
        alt=""
        class="people bgBase"
      />
    </picture>
    <div class="content">
      <Image src={LogoImage} width={200} height={200} alt="" class="logo" />
      <h1 class="eventName">
        <span class="eventNameColumn">フロントエンド</span>
        <span class="eventNameColumn">カンファレンス</span>
        <span class="eventNameColumn">トウキョウ</span>
      </h1>
      <div class="eventInfo">
        <span class="location">In Shibuya, Tokyo - Abema Towers</span>
        <span class="datetime"
          ><time datetime="2025-09-21T12:00+09:00">2025.09.21 SUN 12:00</time
          ><span class="timeSeparator">–</span>19:00</span
        >
      </div>
    </div>
  </div>
</section>

<style>
  @import "../css/utils.css";
  @import "../css/vars.css";

  .container {
    position: relative;
    height: calc(100vh + 120px);
    overflow: hidden;
    isolation: isolate;
  }
  .container::after {
    position: absolute;
    bottom: -61vw;
    left: 50%;
    z-index: var(--intro-decoration-z-index);
    width: 150vw;
    height: 70vw;
    content: "";
    background: var(--bg-light);
    border-radius: 50% 50% 0 0;
    box-shadow: 0px -8px 64px 0px rgba(0, 0, 0, 0.16);
    transform: translateX(-50%);
  }
  .contents {
    position: relative;
    z-index: var(--base-z-index);
    width: 100vw;
    height: 100vh;
  }

  .traffic,
  .tile,
  .bgColor {
    mix-blend-mode: overlay;
  }

  .bgColor {
    background-color: var(--bg-dark);
  }

  .bgBase {
    position: absolute;
    top: 50%;
    left: 50%;
    width: calc(100vmax * 1.4142);
    height: calc(100vmax * 1.4142);
    transform: translate(-50%, -50%) rotate(-45deg);
  }

  .content {
    position: absolute;
    top: 160px;
    left: 50%;
    display: flex;
    flex-direction: column;
    align-items: center;
    color: #fff;
    text-align: center;
    transform: translateX(-50%);
  }

  .eventName {
    display: flex;
    flex-direction: row-reverse;
    gap: 24px;
    align-items: flex-start;
    justify-content: center;
    padding: 0;
    margin-top: 32px;
    font-size: 0;
    line-height: 1;
  }

  .eventNameColumn {
    display: block;
    font-size: clamp(32px, 4vw, 44px);
    font-weight: bold;
    vertical-align: top;
    text-align: start;
    letter-spacing: 0.02em;
    writing-mode: vertical-rl;
    text-orientation: upright;
  }

  .eventInfo {
    display: flex;
    flex-direction: column;
    gap: 12px;
    align-items: center;
    margin-top: 64px;
  }

  .eventInfo .location,
  .eventInfo .datetime {
    font-size: 20px;
    font-weight: bold;
    line-height: 1;
    letter-spacing: 0.01em;
    text-wrap: nowrap;
    text-shadow: 0 2px 4px rgb(0 0 0 / 70%);
  }

  .timeSeparator {
    margin: 0 0.3em;
  }

  @media (--breakpoint-sp) {
    /* TODO: デザイン再現の調整中 */
    .container {
      height: calc(100vh + 30px);
    }
    .bgBase {
      top: 52.4%;
      width: calc(100vmax * 1.1);
      height: calc(100vmax * 1.1);
    }

    .content {
      top: 144px;
    }

    .logo {
      width: 144px;
      height: 144px;
    }

    .eventName {
      gap: 16px;
      margin-top: 20px;
    }

    .eventInfo {
      margin-top: 44px;
    }

    .eventInfo .location,
    .eventInfo .datetime {
      letter-spacing: 0.001em;
    }
  }
</style>
